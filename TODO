.61111
+  Sec: check ../ in URL and job parameters;
+  java agent added, oracle package renamed;
+  echo bash script updated.

-  trusted RESTup servers, load balansing, redirect/proxy...;
-  build-in unzip/zip;
-  spaces in paths;
-  Config: add multipliers (Kilo, Mega, Giga) to jobQuota, (Minutes) to jobsLifeTime, jobTimeOut values; 
-  ? catch and explain init port exceptions (Security, Busy);
-  ? DAV: replace 404 with 410 Gone;
-  ? Log: Windows decode external programs STDERR according consoleEncoding;
