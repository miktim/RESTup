+  correct html error reason phrases according RFC;
+  noop instead error when no jobfiles and jobQuota > 0;
+  take WindowsXP short path from environment var %TEMP%;
+- spaces in paths (?);
-  catch and explain init port exceptions (Security, Busy);
-  DAV: replace 404 with 410 Gone (?);
-  Log: decode external programs STDERR according consoleEncoding;
-  Config: add multipliers (Kilo, Mega, Giga) to jobQuota, (Minutes) to jobsLifeTime, jobTimeOut values; 
+  add build.sh, correct build.bat.
